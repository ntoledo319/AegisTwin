# AegisTwin Kubernetes Service
# AI-GENERATED 2026-01-07

apiVersion: v1
kind: Service
metadata:
  name: {{ include "aegistwin.fullname" . }}
  labels:
    {{- include "aegistwin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aegistwin.selectorLabels" . | nindent 4 }}
